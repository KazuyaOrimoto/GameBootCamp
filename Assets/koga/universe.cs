using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class universe : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Sound.LoadBGM("utyuu", "utyuu");
        Sound.PlayBGM("utyuu");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
